<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512">
  <defs>
    <linearGradient id="grad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" style="stop-color:#0F172A;stop-opacity:1" />
      <stop offset="100%" style="stop-color:#000000;stop-opacity:1" />
    </linearGradient>
    <filter id="glow" x="-20%" y="-20%" width="140%" height="140%">
      <feGaussianBlur stdDeviation="10" result="blur" />
      <feComposite in="SourceGraphic" in2="blur" operator="over" />
    </filter>
  </defs>
  
  <!-- Background -->
  <rect width="512" height="512" rx="160" fill="url(#grad)" />
  
  <!-- Futuristic Minimalist Shards -->
  <g transform="translate(256, 256)" filter="url(#glow)">
    <!-- Top Shard -->
    <path d="M -120 -120 L 80 -120 L -120 80 Z" fill="#3B82F6" opacity="0.8" />
    
    <!-- Bottom Shard -->
    <path d="M 120 120 L -80 120 L 120 -80 Z" fill="#60A5FA" />
    
    <!-- Accent Line (The Split) -->
    <line x1="-140" y1="140" x2="140" y2="-140" stroke="white" stroke-width="8" stroke-linecap="round" opacity="0.3" />
    
    <!-- Center Dot (Focus Point) -->
    <circle cx="0" cy="0" r="12" fill="white" />
  </g>
</svg>
